# 🚀 快速部署指南 - 解决 404 问题

## ⚠️ 404 错误的原因

Cloudflare Pages 出现 404 通常是因为：
1. **文件结构不正确** - index.html 不在根目录
2. **构建输出目录配置错误** - 设置了错误的输出目录

## ✅ 正确的部署方法（3种）

---

### 方法 1️⃣: Git 仓库部署（推荐）

#### 第一步：准备仓库
```bash
# ⚠️ 重要：只推送 cf 文件夹的内容，不要包含 cf 文件夹本身！
cd cf

git init
git add .
git commit -m "Deploy Punch game"
git remote add origin <你的仓库地址>
git push -u origin main
```

#### 第二步：Cloudflare 配置
1. 访问 https://dash.cloudflare.com/
2. **Workers & Pages** → **Create application** → **Pages** → **Connect to Git**
3. 选择你的仓库
4. **关键配置**：
   ```
   项目名称: punch-game
   生产分支: main
   构建命令: （留空）
   构建输出目录: /  ← 重要！必须是根目录
   ```
5. 点击 **Save and Deploy**

---

### 方法 2️⃣: Dashboard 直接上传（最简单）

#### 第一步：准备 ZIP 文件
```bash
# Windows
cd cf
# 选择所有文件 → 右键 → 发送到 → 压缩(zipped)文件夹

# Mac/Linux
cd cf
zip -r ../punch-game.zip .
```

⚠️ **重要**：ZIP 文件解压后应该直接看到 index.html，而不是 cf 文件夹！

#### 第二步：上传部署
1. https://dash.cloudflare.com/
2. **Workers & Pages** → **Create application** → **Pages** → **Upload assets**
3. 项目名称：punch-game
4. 拖拽上传 ZIP 文件
5. **Deploy site**

---

### 方法 3️⃣: Wrangler CLI 部署

```bash
# 安装 Wrangler
npm install -g wrangler

# 登录
wrangler login

# 部署
cd cf
wrangler pages deploy . --project-name=punch-game
```

---

## 🔍 如果还是 404，检查这些：

### 1. 检查 Cloudflare 中的文件结构
在 Cloudflare Dashboard 中：
- 进入你的 Pages 项目
- 点击最新的 **Deployment**
- 查看 **Build output** 或 **Deployment details**
- 确认 index.html 在根目录

✅ **正确的结构**：
```
/
├── index.html
├── game.js
├── style.css
└── assets/
```

❌ **错误的结构**：
```
/
└── cf/
    ├── index.html
    └── ...
```

### 2. 修改构建配置
如果部署错误：
1. 进入项目 **Settings** → **Builds & deployments**
2. 找到 **Build output directory**
3. 改为 `/` 或留空
4. **Retry deployment**

### 3. 删除并重新部署
如果以上都不行：
1. 删除整个 Pages 项目
2. 按照上面的方法重新部署

---

## 📋 快速检查清单

部署前确认：
- [ ] cf 文件夹中有 index.html
- [ ] cf 文件夹中有 game.js 和 style.css
- [ ] cf 文件夹中有 assets 文件夹
- [ ] 如果用 Git：推送的是 cf 文件夹内容，不是 cf 文件夹本身
- [ ] 如果用 ZIP：解压后直接是 index.html 等文件
- [ ] Cloudflare 构建输出目录设置为 `/`

---

## 🎯 验证部署成功

访问 `https://你的项目名.pages.dev`，应该看到：
- ✅ 游戏加载界面
- ✅ "空战传奇" 标题
- ✅ "开始游戏" 按钮

如果看到这些，恭喜！部署成功！🎉

---

## 💡 提示

**推荐部署流程**：
1. 使用 Git 仓库方式（最可靠）
2. 确保只推送 cf 文件夹的内容
3. Cloudflare 配置保持简单：构建输出目录设为 `/`
4. 如果出错，先检查文件结构，再重新部署

**需要帮助？**
- 查看完整文档：README.md
- 检查浏览器控制台（F12）的错误信息
- 确认 Cloudflare Dashboard 中的部署日志